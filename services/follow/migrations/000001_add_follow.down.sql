-- Drop the users table if it exists
DROP TABLE IF EXISTS follow.follow CASCADE;
DROP TABLE IF EXISTS follow.users CASCADE;

-- Drop the users schema if it's now empty
DROP SCHEMA IF EXISTS follow CASCADE;

